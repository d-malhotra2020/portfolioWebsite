# Phase 8 — Deferred (Drew action required)

**Why this isn't fully autonomous:** Phase 8 ships TWO halves: (a) **frontend GA4 event instrumentation** — fully autonomous-eligible, and (b) **GA4 dashboard + Cloudflare Workers Analytics dashboard config** — requires Drew's GA4 console access and Cloudflare dashboard clicks.

The autonomous workflow can complete the code half today. The dashboard half is a 15-minute clickthrough Drew has to do himself.

## Half 1 — Autonomous-eligible work (not yet executed)

Frontend instrumentation in `src/`:
- `gtag('event', 'resume_download', {...})` on the Hero "Download résumé" button + `Resume.jsx` print button
- `gtag('event', 'agent_conversation_start', {...})` on first message sent in `AgentDock.jsx`
- `gtag('event', 'agent_message_sent', {...})` on each subsequent message
- `gtag('event', 'contact_form_submitted', {...})` on `Contact.jsx` form success
- `gtag('event', 'deep_dive_viewed', {slug})` on `WorkPost.jsx` + `WritingPost.jsx` mount

This is ~50 LOC across 5 files. Zero new dependencies (gtag is already loaded in `index.html`). Drew confirms the existing GA4 measurement ID `G-DNGHGRF5MM` is the right destination → autonomous can ship it.

Say "execute Phase 8 frontend instrumentation" in any future session to run just this half.

## Half 2 — Drew action required (the dashboard config)

### 2a. GA4 custom dashboard

1. Open https://analytics.google.com → Property `drewmalhotra.com` → **Reports** → **Library** → **Create new report**.
2. Name: "Recruiter funnel". Add cards for each custom event from Half 1 (5 events). Add a funnel exploration: `agent_conversation_start` → `agent_message_sent` (≥3) → `contact_form_submitted`.
3. Pin to the property home so it loads first.

### 2b. Cloudflare Workers Analytics dashboard

1. Open Cloudflare dashboard → Workers → `agent` Worker → **Metrics**.
2. The telemetry binding already writes data points with blobs `[model, outcome, ip]` and doubles `[inputTokens, outputTokens, costMicro]` (see `workers/agent/src/index.js:309-323`).
3. Build a query that shows: request volume, outcome breakdown (ok / rate_limited / cost_capped / upstream_error), daily $ cost (sum of costMicro / 1000000), P95 latency.
4. Save the query as a named view.

### 2c. Confirm the analytics engine binding is enabled

Per the Phase 10 SUMMARY notes, the `TELEMETRY` Analytics Engine binding may be commented out in `wrangler.toml` until Drew enables it in the Cloudflare dashboard. Confirm:

```bash
grep -n TELEMETRY workers/agent/wrangler.toml
```

If commented, uncomment and `wrangler deploy`.

## Once the above is done

Update this file with: dashboard URLs (GA4 + Cloudflare), confirmation that telemetry binding is live. Then re-run `/gsd:autonomous --only 8`.

---
*Generated by /gsd-autonomous on 2026-05-27.*
