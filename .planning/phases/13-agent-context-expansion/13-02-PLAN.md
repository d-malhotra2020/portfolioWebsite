---
phase: 13-agent-context-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified: []
autonomous: conditional
requirements:
  - REQ-AGENT-13
must_haves:
  truths:
    - "The Worker is deployed to Cloudflare with the expanded SYSTEM_PROMPT and the cache_control-enabled upstream system field"
    - "Two verification curls against the deployed Worker each return a response that cites at least one new entry from the expanded prompt — one Jira-themed question and one GitHub-themed question"
    - "If `wrangler whoami` fails or wrangler is unavailable in the executor's environment, a `DEPLOY-PENDING.md` file exists in the phase directory documenting the exact two commands Drew needs to run, and plan 13-02 is marked `human_needed`"
  artifacts:
    - path: ".planning/phases/13-agent-context-expansion/13-02-SUMMARY.md"
      provides: "Deploy + verification record — wrangler deploy output, curl responses, cache observation notes"
      contains: "13-02"
    - path: ".planning/phases/13-agent-context-expansion/DEPLOY-PENDING.md"
      provides: "Conditional — only created if wrangler auth fails or wrangler is missing; documents the exact deploy + verification commands Drew must run"
      contains: "wrangler deploy"
  key_links:
    - from: "Plan 13-01's atomic commit on `workers/agent/src/index.js`"
      to: "Cloudflare Worker `drew-agent` at the route configured in wrangler.toml (custom domain or *.workers.dev)"
      via: "wrangler deploy from workers/agent/"
      pattern: "wrangler deploy"
    - from: "Deployed Worker's Anthropic upstream call"
      to: "Anthropic Messages API with structured system + cache_control (5-min TTL on the system block)"
      via: "system array with cache_control: { type: 'ephemeral' } from plan 13-01"
      pattern: "cache_control"
---

<objective>
Deploy the Worker with the expanded SYSTEM_PROMPT (shipped in plan 13-01) and verify the agent actually cites the new entries when prompted. If the executor can't authenticate with Cloudflare (no wrangler CLI in PATH or `wrangler whoami` fails), emit a `DEPLOY-PENDING.md` note for Drew with the exact two commands he needs to run, and mark this plan `human_needed` so the orchestrator surfaces the pending action.

Purpose: Phase 13's value lands only when the deployed agent has the new context. Plan 13-01 ships the code change locally; plan 13-02 closes the loop with a live deploy + curl verification that the agent actually surfaces the new Jira/GitHub context to recruiters.

Output: Either a successful deploy + curl-verified response citing the new entries (recorded in `13-02-SUMMARY.md`), OR a `DEPLOY-PENDING.md` runbook for Drew.

Honors Phase 13 CONTEXT decisions: deploy is treated as Drew-action-OR-autonomous-if-creds-present, verification uses the two curl commands locked in CONTEXT § "Verification".
</objective>

<execution_context>
Autonomous, conditional on wrangler auth. The plan's autonomous status flips to `human_needed` if wrangler whoami fails.
</execution_context>

<context>
@.planning/phases/13-agent-context-expansion/13-CONTEXT.md
@workers/agent/wrangler.toml
@workers/agent/src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check wrangler auth status — branch on autonomous vs Drew-action</name>
  <files></files>
  <read_first>
    - workers/agent/wrangler.toml (confirm the deploy target — `drew-agent` Worker, custom domain candidate `agent.drewmalhotra.com`)
  </read_first>
  <action>
    From `workers/agent/`, run `npx wrangler whoami 2>&1`. Capture the output.

    **If wrangler is authenticated** (output includes "You are logged in" and an Account ID): proceed to Task 2 (autonomous deploy + verification).

    **If wrangler is missing or auth fails** (command not found, "not authenticated", or similar): proceed to Task 3 (write DEPLOY-PENDING.md and exit with human_needed status).
  </action>
  <verify>
    <automated>cd workers/agent && npx wrangler whoami 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The executor has captured wrangler's auth state and branched correctly to either Task 2 (deploy) or Task 3 (DEPLOY-PENDING).
  </acceptance_criteria>
  <done>
    The auth state is known; the executor proceeds down exactly one of the two branches.
  </done>
</task>

<task type="auto">
  <name>Task 2 (autonomous branch): wrangler deploy + verification curls</name>
  <files></files>
  <read_first></read_first>
  <action>
    From `workers/agent/`, run `npx wrangler deploy 2>&1`. Capture the deployed Worker URL (custom domain like `agent.drewmalhotra.com` if mapped, otherwise the `*.workers.dev` URL from the deploy output).

    Run the two verification curls from CONTEXT.md § "Verification" against the deployed Worker (substitute the resolved URL):

    Curl 1 — GitHub-themed prompt:
    ```
    curl -s -X POST <WORKER_URL> \
      -H 'Content-Type: application/json' \
      -d '{"messages":[{"role":"user","content":"What private repos have you shipped to recently?"}]}' | head -c 2000
    ```

    Curl 2 — Jira-themed prompt:
    ```
    curl -s -X POST <WORKER_URL> \
      -H 'Content-Type: application/json' \
      -d '{"messages":[{"role":"user","content":"Tell me a Jira ticket where you found a bug nobody else caught."}]}' | head -c 2000
    ```

    For each response, confirm the SSE stream cites at least one entry from the new sections (e.g. an `EEPD-XXXXX` ticket ID for curl 2, or a private-repo name like `qalab-alertMonitor` / `test-tools` / `api-v3-documentation` / `qa-webhook-server` for curl 1).

    Record the first ~300 chars of each response in `13-02-SUMMARY.md`. If a response doesn't cite a new entry, mark it as a soft-fail in the summary — the agent may have chosen a different angle that's still on-topic. Hard-fail (status: gaps_found) only if BOTH curls return errors or are clearly off-topic.

    Optional: re-issue curl 1 after ~30 seconds to give the cache a chance to warm. Note any observable latency change. Cache_read_input_tokens are internal to Anthropic billing — don't expect to see them in the SSE stream; the existence of the cache_control in the request is the proof.

    Confirm the rate-limit and daily-cost breaker still respond (don't trip them — just verify the Worker didn't regress by visiting the deployed URL in a browser via `https://drewmalhotra.com` and seeing the agent dock load).
  </action>
  <verify>
    <automated>cd workers/agent && npx wrangler deploy 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `wrangler deploy` exits 0.
    - Both verification curls return non-empty SSE responses.
    - At least one of the two responses cites a new prompt entry (a new EEPD ticket ID or a new private-repo name).
    - The agent's existing capabilities (rate limit, cost breaker) are not visibly regressed.
  </acceptance_criteria>
  <done>
    The expanded prompt is live on the production Worker, verification curls confirm the agent surfaces the new context, and `13-02-SUMMARY.md` records the deploy output + curl responses.
  </done>
</task>

<task type="auto">
  <name>Task 3 (human_needed branch): write DEPLOY-PENDING.md</name>
  <files>.planning/phases/13-agent-context-expansion/DEPLOY-PENDING.md</files>
  <read_first></read_first>
  <action>
    Create `.planning/phases/13-agent-context-expansion/DEPLOY-PENDING.md` with the exact two commands Drew needs to run, followed by the two verification curls from CONTEXT.md.

    Content template:

    ```
    # Phase 13 — Deploy pending

    Plan 13-01 shipped the SYSTEM_PROMPT expansion + prompt caching. The Worker has NOT been deployed because the executor's environment lacks Cloudflare auth.

    ## Drew action — one command:

        cd workers/agent && npx wrangler deploy

    ## Verification — two curls:

        curl -s -X POST https://agent.drewmalhotra.com \
          -H 'Content-Type: application/json' \
          -d '{"messages":[{"role":"user","content":"What private repos have you shipped to recently?"}]}' | head -c 2000

        curl -s -X POST https://agent.drewmalhotra.com \
          -H 'Content-Type: application/json' \
          -d '{"messages":[{"role":"user","content":"Tell me a Jira ticket where you found a bug nobody else caught."}]}' | head -c 2000

    Confirm: each response cites at least one new entry from the expanded prompt
    (a new EEPD ticket ID for the Jira question, or a private repo name like
    qalab-alertMonitor / test-tools / api-v3-documentation for the GitHub
    question, or `qa-webhook-server` for the public callout).
    ```

    Mark plan 13-02 as `human_needed` in `13-VERIFICATION.md` (handled in plan-close).
  </action>
  <verify>
    <automated>test -f .planning/phases/13-agent-context-expansion/DEPLOY-PENDING.md && grep -q "wrangler deploy" .planning/phases/13-agent-context-expansion/DEPLOY-PENDING.md</automated>
  </verify>
  <acceptance_criteria>
    - `DEPLOY-PENDING.md` exists at the right path.
    - It contains the exact `cd workers/agent && npx wrangler deploy` command.
    - It contains both verification curls verbatim from CONTEXT.md.
  </acceptance_criteria>
  <done>
    Drew can open `DEPLOY-PENDING.md` and finish the deploy + verification in under 2 minutes.
  </done>
</task>

</tasks>

<verification>
  <human-check>
    - If autonomous branch: verify by visiting `https://drewmalhotra.com` and asking the agent dock either of the two verification questions; confirm it cites a new entry.
    - If human_needed branch: Drew runs the two commands in `DEPLOY-PENDING.md` and pastes the curl outputs back into the SUMMARY.
  </human-check>
</verification>

<success_criteria>
- Either: (a) Worker deployed successfully AND both verification curls each returned a non-empty response, with at least one response citing a new prompt entry, recorded in `13-02-SUMMARY.md`; OR (b) `DEPLOY-PENDING.md` exists with the exact commands Drew needs, and plan 13-02 is marked `human_needed` in `13-VERIFICATION.md`.
- No file under `workers/agent/` is modified by this plan (deploy reads from the existing source; verification curls don't write to the repo).
</success_criteria>

<output>
Create `.planning/phases/13-agent-context-expansion/13-02-SUMMARY.md` when done.
</output>
