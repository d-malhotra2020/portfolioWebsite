---
phase: 13-agent-context-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/agent/src/index.js
autonomous: true
requirements:
  - REQ-AGENT-13
must_haves:
  truths:
    - "workers/agent/src/index.js SYSTEM_PROMPT contains two new sections appended after the existing `# Brivo / EEN — named projects` block: `# Additional Brivo Jira — supplementary named tickets` (8 entries) and `# GitHub — shipped work` (6 entries)"
    - "Every Jira entry follows the locked format `- **EEPD-XXXXX <title>** — <one-line outcome>.` and is sourced from 13-CANDIDATES-jira.md candidate pool (no fabrication, no customer names)"
    - "Every GitHub entry follows the locked format `- **<PR title>** — EENCloud/<repo> (private), merged <YYYY-MM-DD>. <one-line outcome>.` for private PRs and `- **<repo>** — <description>. github.com/d-malhotra2020/<repo>` for the public callout"
    - "The upstream `fetch` call's `system` field is switched from the bare string `SYSTEM_PROMPT` to the structured array form with `cache_control: { type: 'ephemeral' }`, enabling Anthropic prompt caching with a 5-minute TTL"
    - "`node --check workers/agent/src/index.js` exits 0 — every backtick inside SYSTEM_PROMPT remains properly escaped"
    - "No file outside workers/agent/src/index.js is touched in this plan"
  artifacts:
    - path: "workers/agent/src/index.js"
      provides: "Two new sections in SYSTEM_PROMPT — Additional Brivo Jira (8 entries) + GitHub (6 entries)"
      contains: "Additional Brivo Jira"
    - path: "workers/agent/src/index.js"
      provides: "Structured system field with cache_control on the upstream fetch call"
      contains: "cache_control"
  key_links:
    - from: "workers/agent/src/index.js (SYSTEM_PROMPT — # Brivo / EEN — named projects block, around line 71)"
      to: "workers/agent/src/index.js (SYSTEM_PROMPT — # Additional Brivo Jira + # GitHub sections, new)"
      via: "narrative ordering — named projects (depth) → supplementary Jira (breadth) → GitHub (shipped artifacts) → next career entry (Yunex)"
      pattern: "Additional Brivo Jira"
    - from: "workers/agent/src/index.js fetch handler (system field)"
      to: "Anthropic Messages API prompt caching (5-min TTL on the system block, 10% cost on cached reads)"
      via: "system array with cache_control: { type: 'ephemeral' }"
      pattern: "cache_control"
---

<objective>
Curate exactly 8 Jira entries from `13-CANDIDATES-jira.md` and exactly 6 GitHub entries from `13-CANDIDATES-github.md`, append them as two new sections to the `SYSTEM_PROMPT` template literal in `workers/agent/src/index.js`, and switch the upstream `fetch` call's `system` field to the structured array form with `cache_control: { type: 'ephemeral' }` to enable Anthropic prompt caching.

Purpose: The interview agent currently knows the named Brivo projects + the public side projects, but does NOT cite the broader Jira corpus (bug-hunting depth, process maturity, CI lifecycle work) or the steady cadence of merged GitHub PRs. Phase 13 expands that context. Caching ensures the +1,800-2,400 token growth doesn't push per-request cost past the daily-cost circuit breaker established in Phase 10 — cached reads cost 10% of normal input cost, so the second+ request in any 5-minute window pays ~10x less for the system block.

Output: A single commit to `workers/agent/src/index.js` containing both the prompt expansion and the cache_control wiring. No other files modified. The bundle remains syntactically valid JS.

Implements REQ-AGENT-13. Honors Phase 13 CONTEXT decisions: 8 Jira + 6 GitHub entries (locked), append after named-projects block (locked), customer-name redaction non-negotiable, anti-fabrication rule from commit fe0e526 non-negotiable, structured system field with ephemeral cache_control (locked).
</objective>

<execution_context>
Autonomous execution. Source-of-truth files:
- `.planning/phases/13-agent-context-expansion/13-CONTEXT.md` — all decisions locked
- `.planning/phases/13-agent-context-expansion/13-CANDIDATES-jira.md` — Jira pool + selection guidance
- `.planning/phases/13-agent-context-expansion/13-CANDIDATES-github.md` — GitHub pool + selection guidance
</execution_context>

<context>
@.planning/phases/13-agent-context-expansion/13-CONTEXT.md
@.planning/phases/13-agent-context-expansion/13-CANDIDATES-jira.md
@.planning/phases/13-agent-context-expansion/13-CANDIDATES-github.md
@workers/agent/src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate 8 Jira entries + 6 GitHub entries from the staged candidate pools</name>
  <files></files>
  <read_first>
    - .planning/phases/13-agent-context-expansion/13-CANDIDATES-jira.md (full — selection guidance at the bottom is binding)
    - .planning/phases/13-agent-context-expansion/13-CANDIDATES-github.md (full — selection guidance at the bottom is binding)
  </read_first>
  <action>
    Pick exactly 8 Jira entries spanning these failure-mode categories (per CANDIDATES-jira.md selection guidance):
    1. Input-validation bug (EEPD-117237 — `imageOptions` 500 vs 400)
    2. State-cleanup bug (EEPD-108893 — notifications sent after rule deleted)
    3. Cross-account / cascading-state bug (EEPD-96197 — disable AlertAction fails when user deleted)
    4. i18n / localization bug (EEPD-106035 — time zone not reflecting in email, OR EEPD-105889)
    5. Dedup / idempotency bug (EEPD-92085 — multiple alerts for same event)
    6. Process maturity (EEPD-74866 — API traceability matrix)
    7. CI / pipeline maturity (EEPD-75169 — pytest migration, OR EEPD-103925 — PR/deploy/E2E ownership)
    8. Data lifecycle (EEPD-80160 — video-retention purging)

    Pick exactly 6 GitHub entries (5 private PRs + 1 public repo callout) following the CANDIDATES-github.md selection guidance:
    1. Productization scale — qalab-alertMonitor PR #1 (19,309-line initial implementation, merged 2026-05-11)
    2. Post-ship hardening — qalab-alertMonitor PR #4 (+875/-1368 across 31 files, merged 2026-05-26)
    3. LLM-tooling in production QA — test-tools PR #25 (Claude Context System — QA Integration Edition, merged 2026-02-11)
    4. API contract correctness — api-v3-documentation PR #1400 (PATCH /alertActions documentation, merged 2025-12-10)
    5. Test infrastructure — concourse-pipelines PR #2684 (unblock deployment by quarantining flaky test, merged 2025-05-14)
    6. Public artifact — `qa-webhook-server` (public sibling to private EENCloud/test-tools webhook server)

    Compose the two prompt blocks using the locked formats from CONTEXT.md § "Decisions locked":

    **Block 1 — `# Additional Brivo Jira — supplementary named tickets`**
    - Opens with a 1-2 sentence framing: "These complement the named projects above — they show the *breadth* of QA ownership across the EEPD project beyond the highlighted work."
    - 8 entries, each format: `- **EEPD-XXXXX <plain-English title>** — <one-line outcome / why it mattered>.`
    - Customer names: redact (none of the 8 selected entries reference customer names in the candidate file, so this is a check-not-a-fix).

    **Block 2 — `# GitHub — shipped work`**
    - Opens with a 1-2 sentence framing: "Drew's GitHub work spans public side projects (listed above) and a steady cadence of merged PRs in private Brivo repos. A representative slice:"
    - 5 private PR entries, each format: `- **<PR title>** — EENCloud/<repo> (private), merged <YYYY-MM-DD>. <one-line outcome>.`
    - 1 public repo entry, format: `- **<repo>** — <description>. github.com/d-malhotra2020/<repo>`
    - Private repo URLs: omit (locked rule).
    - Fabricated impact numbers: NEVER (use only metrics in the PR title or candidate file).

    Honesty rule: every claim traces back to the candidate file. If a metric isn't there, drop the metric — write "validated" or "shipped" instead of an invented percentage.
  </action>
  <verify>
    <automated>echo "manual curation — verified by Task 2 grep checks"</automated>
  </verify>
  <acceptance_criteria>
    - The curated lists exist (in the executor's working memory) as two pre-formatted blocks of Markdown bullets.
    - Each Jira entry maps to exactly one EEPD ticket ID from the candidate pool.
    - Each GitHub entry maps to exactly one PR or repo from the candidate pool.
    - No customer names, no private-repo URLs, no fabricated metrics.
  </acceptance_criteria>
  <done>
    Two blocks of Markdown bullets are composed and ready to be inserted into the SYSTEM_PROMPT template literal in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert the two new sections into SYSTEM_PROMPT in workers/agent/src/index.js</name>
  <files>workers/agent/src/index.js</files>
  <read_first>
    - workers/agent/src/index.js (lines 28-152 — full SYSTEM_PROMPT)
  </read_first>
  <action>
    Locate the end of the existing `# Brivo / EEN — named projects` block. The block ends with the "How to use this section:" paragraph (around line 72) and is followed by a blank line + the next career entry `- 2023.07 – 2024.10: Software Engineer, Yunex Traffic, Austin TX` (around line 74).

    Insert the two new sections at that exact boundary — after the "How to use this section:" paragraph, BEFORE the Yunex career entry. The narrative flow becomes:
    1. `# Brivo / EEN — named projects` (existing — depth on flagship work)
    2. `# Additional Brivo Jira — supplementary named tickets` (NEW — breadth across the EEPD project)
    3. `# GitHub — shipped work` (NEW — proof of shipped artifacts, both public and private)
    4. Yunex Traffic career entry (existing — continues the chronological resume)

    Insert via a single Edit call against the exact end-of-block + start-of-next-entry text.

    Backtick discipline: any literal backtick that appears inside the SYSTEM_PROMPT template literal MUST be escaped with a backslash. Cross-check the curated bullets for any backticks (none expected in the Jira or GitHub entries, but verify).
  </action>
  <verify>
    <automated>grep -n "# Additional Brivo Jira" workers/agent/src/index.js && grep -n "# GitHub — shipped work" workers/agent/src/index.js && grep -c "EEPD-" workers/agent/src/index.js && grep -n "qa-webhook-server" workers/agent/src/index.js && node --check workers/agent/src/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "# Additional Brivo Jira" workers/agent/src/index.js` returns exactly 1 match.
    - `grep -n "# GitHub — shipped work" workers/agent/src/index.js` returns exactly 1 match.
    - `grep -c "EEPD-" workers/agent/src/index.js` returns at least 15 matches (existing references + 8 new entries — existing prompt already cites a handful of EEPDs).
    - `grep -n "qa-webhook-server" workers/agent/src/index.js` returns exactly 1 match (the public repo callout).
    - `node --check workers/agent/src/index.js` exits 0.
    - The two new sections sit between `# Brivo / EEN — named projects` (existing) and the Yunex career entry.
  </acceptance_criteria>
  <done>
    SYSTEM_PROMPT now contains the 8 Jira + 6 GitHub entries in the locked order, the file parses as valid JS, and the narrative flow reads top-to-bottom without discontinuity.
  </done>
</task>

<task type="auto">
  <name>Task 3: Switch the upstream `system` field to the structured array form with cache_control</name>
  <files>workers/agent/src/index.js</files>
  <read_first>
    - workers/agent/src/index.js (lines ~437-451 — the upstream `fetch` call)
  </read_first>
  <action>
    Locate the `fetch('https://api.anthropic.com/v1/messages', ...)` call inside the `fetch` handler. In its JSON body, replace:

    ```js
    system: SYSTEM_PROMPT,
    ```

    with:

    ```js
    system: [
      {
        type: 'text',
        text: SYSTEM_PROMPT,
        cache_control: { type: 'ephemeral' }
      }
    ],
    ```

    This is the exact form locked in CONTEXT.md § "Prompt caching (Anthropic native)". The `anthropic-version: 2023-06-01` header already supports prompt caching (GA) — no header change needed. No telemetry changes needed: the existing `teeWithUsage` parses `message_start.usage.input_tokens` which already includes cache_creation + cache_read tokens in the input total.

    Single Edit call.
  </action>
  <verify>
    <automated>grep -n "cache_control: { type: 'ephemeral' }" workers/agent/src/index.js && grep -nE "^\s+system: \[" workers/agent/src/index.js && grep -c "^\s+system: SYSTEM_PROMPT," workers/agent/src/index.js | grep -E '^0$' && node --check workers/agent/src/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cache_control: { type: 'ephemeral' }" workers/agent/src/index.js` returns exactly 1 match.
    - The bare `system: SYSTEM_PROMPT,` form is gone (0 occurrences).
    - `node --check workers/agent/src/index.js` exits 0.
    - No other field in the upstream fetch body changes (model, max_tokens, messages, stream are byte-identical).
  </acceptance_criteria>
  <done>
    The upstream call now sends the system prompt in the structured array form with ephemeral cache_control, enabling Anthropic prompt caching with a 5-minute TTL on the system block.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final syntax check + atomic commit</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run `node --check workers/agent/src/index.js` to confirm the file parses. If it fails, debug the backtick escape or the template-literal interpolation and re-edit.

    Stage and commit the changes with the message:
    `feat(agent): expand SYSTEM_PROMPT with Jira + GitHub context, enable prompt caching`

    Use a HEREDOC for the commit message body so the Co-Authored-By trailer renders correctly. Only `workers/agent/src/index.js` should be in the diff.
  </action>
  <verify>
    <automated>node --check workers/agent/src/index.js && git log -1 --format='%s' | grep -q "expand SYSTEM_PROMPT" && git diff --name-only HEAD~1 HEAD | grep -v '^workers/agent/src/index.js$' | wc -l | grep -q '^[[:space:]]*0$'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check workers/agent/src/index.js` exits 0.
    - Last commit subject contains "expand SYSTEM_PROMPT".
    - `git diff --name-only HEAD~1 HEAD` returns exactly `workers/agent/src/index.js`.
  </acceptance_criteria>
  <done>
    A single atomic commit lands the prompt expansion + cache_control wiring. The working tree is clean. Plan 13-02 can take over for deploy + verification.
  </done>
</task>

</tasks>

<verification>
  <human-check>
    After this plan ships:
    - `node --check workers/agent/src/index.js` exits 0.
    - `git log -1` shows the commit subject `feat(agent): expand SYSTEM_PROMPT with Jira + GitHub context, enable prompt caching`.
    - The diff is exactly `workers/agent/src/index.js`. No README, no wrangler.toml, no frontend.
    - The two new section headings (`# Additional Brivo Jira — supplementary named tickets`, `# GitHub — shipped work`) appear in `workers/agent/src/index.js` in the order specified.
    - The structured `system: [...]` form with `cache_control: { type: 'ephemeral' }` is present in the upstream fetch body.
  </human-check>
</verification>

<success_criteria>
- SYSTEM_PROMPT contains the two new section headings, in order, with format compliant to CONTEXT.md's locked rules.
- 8 EEPD ticket entries + 6 GitHub entries (5 private PRs + 1 public repo), all sourced from the candidate files.
- The upstream `fetch` call sends `system` as a structured array with `cache_control: { type: 'ephemeral' }`.
- `node --check workers/agent/src/index.js` exits 0.
- Only `workers/agent/src/index.js` is in the commit diff.
- Commit message follows the existing convention.
</success_criteria>

<output>
Create `.planning/phases/13-agent-context-expansion/13-01-SUMMARY.md` when done.
</output>
