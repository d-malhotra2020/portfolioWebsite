# Phase 11 — Deferred (Drew action required)

**Why this isn't autonomous:** The success criteria literally read "Drew has manually tested all interactive elements on at least one iPhone and at least one Android device" and "landscape orientation works without visual breakage." That's bare-handed device testing — autonomous mode can't hold a phone.

## Half 1 — Autonomous-eligible prep work (not yet executed)

The autonomous run CAN do this without devices:

1. **Static audit pass:** grep every `onClick` / `onTouchStart` in `src/` and verify touch targets are ≥ 44×44 px via the CSS. Produces a `MOBILE-AUDIT-STATIC.md` listing every interactive element with measured target size.
2. **Breakpoint review:** parse all media queries in `src/styles/` and produce a `BREAKPOINT-MAP.md` showing what changes at 480 / 768 / 1024 / 1280px.
3. **iOS-Safari known-issues checklist:** generate a checklist of common iOS Safari gotchas (`100vh` bug, `position: fixed` + keyboard, scroll-locking when modal open, momentum scroll on `overflow: auto`) and flag any matches in the codebase.
4. **AgentDock keyboard interaction analysis:** static review of the AgentDock component for iOS keyboard layout regressions (specifically: does the input remain visible above the keyboard? does the dock scroll-lock correctly?).

This work is ~1 hour and produces 4 markdown files. It gives Drew a targeted "fix these N things, then re-test on devices" list — much faster than a cold device walkthrough.

Say "execute Phase 11 static audit" in any future session to run just this half.

## Half 2 — Drew action required (the actual device pass)

### 2a. iOS test (your iPhone)

Open https://drewmalhotra.com on Safari (not Chrome) and walk through:
- [ ] Hero CTAs are tappable without zoom (≥ 44px tall)
- [ ] AgentDock opens, keyboard pushes up correctly, input visible above keyboard
- [ ] Send a message in the agent — response streams without layout shift
- [ ] Scroll through every section without horizontal-scroll bleed
- [ ] Rotate to landscape mid-scroll — no broken layout
- [ ] Long-press a code block in a deep-dive — text-selection works, no native menu interference
- [ ] Open `/resume` — print stylesheet renders correctly in iOS Safari's print preview
- [ ] Keyboard shortcuts (`?`, `g a`, etc.) — confirm they DON'T fire on touch devices (no virtual-keyboard side effects)

### 2b. Android test (any Android Chrome device)

Same checklist as iOS. Specific Android risks:
- [ ] On-screen back-gesture doesn't conflict with the AgentDock close-swipe
- [ ] Status bar color matches the operator-console dark theme

### 2c. Record findings

For anything broken, file a brief note in this file under a "## Findings" heading:
- Issue (1 sentence)
- Where (file:line if known)
- Severity (blocking ship vs cosmetic)

## Once the above is done

Re-run `/gsd:autonomous --only 11` and it will execute the static audit (if not already done) and then your filed fixes.

---
*Generated by /gsd-autonomous on 2026-05-27.*
