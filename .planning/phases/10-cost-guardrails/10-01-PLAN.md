---
phase: 10-cost-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/agent/README.md
autonomous: true
requirements:
  - REQ-07
must_haves:
  truths:
    - "workers/agent/README.md documents the exact Anthropic dashboard steps Drew must click to set a $10/month spend cap"
    - "The README documents email alert thresholds (50% / 90%) so Drew gets warned before the hard cap fires"
    - "A future-Drew can re-do the dashboard configuration from the README alone, six months from now, without re-deriving the steps"
    - "The README clearly labels this as a Drew-action (no code is changed) and explains how it complements the Worker breaker in 10-02"
  artifacts:
    - path: "workers/agent/README.md"
      provides: "Anthropic dashboard configuration section — 5 numbered steps + screenshot placeholder"
      contains: "Anthropic dashboard configuration"
  key_links:
    - from: "workers/agent/README.md (Anthropic dashboard configuration section)"
      to: "workers/agent/README.md (Cost guardrails section, added by 10-02)"
      via: "section ordering — dashboard cap is the hard wall, Worker breaker is the early warning; the two-layer story is told end-to-end in one README"
      pattern: "Anthropic dashboard configuration"
---

<objective>
Document the Anthropic console steps Drew must perform by hand to put a $10/month spend cap on the API key the Worker uses, with email alerts at 50% and 90%. This is the **hard wall** in the two-layer cost defense; the Worker daily-cost circuit breaker (10-02) is the early warning that ideally trips first.

Purpose: A code-only breaker is not enough — if Drew's API key leaks or if a bug in the Worker ever lets traffic through, the Anthropic dashboard cap is what stops a viral day from running the bill past $5. Future-Drew needs the steps written down so he can re-do this when he rotates the API key or starts a new month.

Output: A new "Anthropic dashboard configuration" section appended to `workers/agent/README.md`, sized to be re-followable in under five minutes.

Implements REQ-07. Honors Phase 10 CONTEXT decisions: monthly cap = $5 (locked), email alerts at 50% / 90% (locked), screenshot placeholder for Drew to fill in after the dashboard is configured (locked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cost-guardrails/10-CONTEXT.md
@workers/agent/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the Anthropic dashboard configuration section to workers/agent/README.md</name>
  <files>workers/agent/README.md</files>
  <read_first>
    - workers/agent/README.md (full file — required to find a clean append location and match heading style + tone)
    - .planning/phases/10-cost-guardrails/10-CONTEXT.md (the "Anthropic dashboard cap — Drew action" subsection is the authoritative source for the dashboard steps)
  </read_first>
  <action>
    Append a new top-level section titled `## Anthropic dashboard configuration` to `workers/agent/README.md`. Place it at the very end of the file so that 10-02's `## Cost guardrails` section can land below it and the two sections together tell the two-layer-defense story end-to-end. The section MUST contain, in order:

    1. A one-paragraph framing that says the Anthropic dashboard cap is the **hard wall** in the cost defense and that the Worker daily-cost circuit breaker (documented in the "Cost guardrails" section below — yet to land in 10-02) is the **early warning**. Mention that this is a one-time Drew-action, not a code change, and that future-Drew should re-run it whenever the API key is rotated or a new Anthropic project is created.

    2. A numbered list of exactly the five steps from `10-CONTEXT.md` § "Anthropic dashboard cap — Drew action":
       1. Sign in at `https://console.anthropic.com/settings/billing`.
       2. Under "Spend limits", set "Monthly spend cap" = **$5**.
       3. Enable email alerts at **50% ($2.50)** and **90% ($4.50)**.
       4. Confirm the configured cap by running `npx wrangler tail` (from `workers/agent/`) while sending a real chat from the deployed site, and watch for an `outcome=ok` telemetry event with a non-zero cost — confirms the API key is the one the dashboard cap applies to.
       5. Replace the screenshot placeholder below with a screenshot of the configured cap page (drag-and-drop into the README on GitHub, or commit a PNG to `workers/agent/docs/` and link it).

    3. A literal screenshot placeholder line (Markdown comment form so it renders as plain text but is obviously a placeholder): `[Anthropic dashboard cap screenshot — Drew adds]`

    4. A short closing paragraph that points the reader at the "Cost guardrails" section below for the in-Worker counterpart: "See **Cost guardrails** below for the Worker-side daily breaker that fires before this dashboard cap does."

    Voice + format requirements:
    - Match the existing README voice — informal, second-person, lower-case headings inside lists are fine, "drew" / "future-Drew" is the established usage.
    - Use the same heading depth (`##`) as the existing top-level sections (e.g. `## Rate limiting`, `## Cost telemetry`).
    - Do not modify any existing section, do not change line endings outside the appended block, and do not touch any file other than `workers/agent/README.md`.
    - No code is being changed — do NOT edit `workers/agent/src/index.js` or `workers/agent/wrangler.toml` in this plan.

    Per Phase 10 CONTEXT decisions (`10-CONTEXT.md` § "Decisions locked" → "Anthropic dashboard cap — Drew action"): $5/mo cap, 50% + 90% alerts, screenshot placeholder.
  </action>
  <verify>
    <automated>grep -n "Anthropic dashboard configuration" workers/agent/README.md &amp;&amp; grep -n "Monthly spend cap" workers/agent/README.md &amp;&amp; grep -nE "(\\$2\\.50|\\$4\\.50)" workers/agent/README.md &amp;&amp; grep -n "Anthropic dashboard cap screenshot" workers/agent/README.md &amp;&amp; git diff --stat workers/agent/README.md | grep -v "src/index.js" | grep -v "wrangler.toml"</automated>
  </verify>
  <acceptance_criteria>
    - `workers/agent/README.md` contains a `## Anthropic dashboard configuration` heading.
    - The section lists all 5 numbered steps (sign-in URL, $5 cap, 50% + 90% alerts, wrangler-tail confirmation, screenshot replacement).
    - The screenshot placeholder string `[Anthropic dashboard cap screenshot — Drew adds]` is present.
    - The section ends by pointing the reader at the "Cost guardrails" section for the Worker-side counterpart.
    - No file other than `workers/agent/README.md` is modified by this plan (`git diff --name-only` returns exactly `workers/agent/README.md`).
    - No code changes — `workers/agent/src/index.js` and `workers/agent/wrangler.toml` are byte-identical to their pre-plan state.
  </acceptance_criteria>
  <done>
    A future-Drew, six months from now, can open `workers/agent/README.md`, find the "Anthropic dashboard configuration" section, and reconfigure the cap from scratch in under five minutes without referencing any other source. The screenshot placeholder is in place for Drew to fill in after he clicks through the dashboard.
  </done>
</task>

</tasks>

<verification>
  <human-check>
    After this plan ships, Drew (separately, as a Drew-action) opens `https://console.anthropic.com/settings/billing`, follows steps 1–4 in the README, confirms the cap is set, captures a screenshot, and commits it in place of the placeholder. The verification command above does NOT check that Drew actually clicked through the dashboard — only that the README documents the steps. Drew's dashboard click is tracked separately in STATE.md "Drew actions waiting".
  </human-check>
</verification>

<success_criteria>
- `## Anthropic dashboard configuration` section exists in `workers/agent/README.md`.
- All five numbered dashboard steps documented verbatim from `10-CONTEXT.md`.
- $5/mo cap, 50% + 90% alert thresholds, screenshot placeholder all present.
- Section explicitly frames itself as the "hard wall" half of the two-layer defense (the Worker breaker in 10-02 being the "early warning").
- No code files touched.
- README still renders cleanly as Markdown (no broken heading hierarchy, no orphaned list items).
</success_criteria>

<output>
Create `.planning/phases/10-cost-guardrails/10-01-SUMMARY.md` when done.
</output>
