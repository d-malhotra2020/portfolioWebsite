# Phase 6 — Deferred (Drew action required)

**Why this isn't autonomous:** This phase ships **third-party content** (LinkedIn recommendations, certifications, talk links). The content itself can only come from real humans Drew has worked with. The autonomous workflow cannot generate trust-signals on Drew's behalf — that would directly violate the project's "no fabricated claims" rule (see `src/writing/honesty-playbook.md` and the existing SYSTEM_PROMPT's anti-fabrication block).

## What Drew needs to do before this phase can run

### 1. Collect 3-5 LinkedIn recommendations (the blocker)

**Suggested template message** (paste into LinkedIn DM):

> Hey {name} — I'm refreshing my portfolio site (drewmalhotra.com) and adding a section for third-party voices. Would you be willing to leave a short LinkedIn recommendation? Even 2-3 sentences on what we worked on at {company} would help a lot. Here's the link: linkedin.com/in/drewmalhotra → Add recommendation.
>
> Happy to write one for you too. No pressure either way — appreciate the consideration.

**Recommended request order** (most likely to write strong recs first):
1. Direct manager at Brivo (current employer — strongest signal for new roles)
2. Direct manager at Yunex Traffic (most recent prior role)
3. Direct manager or PM at Givelify (PyTorch recommender ship)
4. Senior peer / tech lead at Brivo who reviewed your PRs
5. Direct manager at Nourtek (oldest — only ask if 1-4 are slow)

**Target:** 3 published recs. The section won't render publicly with < 3 to avoid a "WIP" look.

### 2. Confirm or skip the certifications/credentials surface

Drew currently has **no** professional certifications on record (AWS, GCP, K8s, etc.). The autonomous interpretation is: surface what IS verifiable — **US Navy service (5 years), UTD CS degree (2021), Brivo employer-confirmed role** — framed as "background verification," not "certs."

Confirm this framing (or veto) before plan-phase runs. Options:
- (a) **Accept** the background-verification framing → proceed.
- (b) **Add a cert push** → block phase until Drew earns ≥1 cert (AWS SAA is the lowest-effort high-recognition option for SDE roles).
- (c) **Skip credentials entirely** → only show LinkedIn quotes; no second "verifiable credential" success criterion.

### 3. Optional: gather any conference / podcast / publication links

Currently zero on record. If anything exists (even an internal Brivo lunch-and-learn recording shareable externally), drop the URL in this file and the autonomous run will surface it. If not, leave blank — the section degrades gracefully.

## Once the above is done

Update this file with: (a) LinkedIn rec URLs (at least 3), (b) credentials framing choice (a/b/c), (c) any links. Then re-run `/gsd:autonomous --only 6` and it will run end-to-end.

## What the autonomous run CAN do today (not yet executed)

If Drew wants the **UI scaffold ready** so the section auto-renders when content arrives:
- Add `src/data/recommendations.js` (empty array, typed shape: `{name, role, relationship, quote, linkedinUrl}`)
- Add `src/components/Trust.jsx` with 3-card grid in the operator-console aesthetic (mono pull-quote, terminal-card chrome, `>` block-quote sigils)
- Wire into `App.jsx` between `<Experience />` and `<Projects />` — but gated on `recommendations.length >= 3` so it's invisible until ready
- Same data shape for credentials in `src/data/credentials.js`

Say "scaffold Phase 6 UI" in any future session to trigger that work without the content blocker.

---
*Generated by /gsd-autonomous on 2026-05-27 to keep the workflow moving past human-content blockers.*
